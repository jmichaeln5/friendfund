<nav class="navbar navbar-expand-lg navbar-dark bg-gray-800">
  <div class="flex-shrink-0 flex items-center">
    <% if current_user %>
      <a href="/dashboard" class="text-white px-3 py-2 rounded-md text-2xl font-medium">FriendFund</a>
    <% else %>
      <a href="/" class="text-white px-3 py-2 rounded-md text-2xl font-medium">FriendFund</a>
    <% end %>

  </div>

  <%= link_to 'admin friend request', admin_friend_request_path, class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium'  %>


  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to 'home', root_path, class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !user_signed_in? %>
      </li>
      <li class="nav-item">
        <%= link_to 'about', pages_about_path, class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !user_signed_in? %>
      </li>
      <li class="nav-item">
        <%= link_to 'dashboard', dashboard_path, class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if user_signed_in? %>
      </li>
      <li class="nav-item">
        <%= link_to 'Users#index', users_path, class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if user_signed_in? %>
      </li>

      <li class="nav-item">
        <%= link_to 'friend requests', user_friend_requests_path(current_user), class:'nav-link text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if user_signed_in? %>
      </li>


    </ul>

    <li class="nav-item">
      <% if current_user %>
        <li class="dropdown">
          <a class=" dropdown-toggle text-gray-300 hover:bg-gray-700 hover:text-white rounded-md text-sm font-medium" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.name.familiar %>
          </a>
          <div class="dropdown-menu " aria-labelledby="navbarDropdown">
            <%= link_to "Test Root Path", root_path, class:" dropdown-item text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>

            <div class="dropdown-divider"></div>

            <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"dropdown-item text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Account Settings", edit_user_registration_path, class:" dropdown-item text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          </div>
        </li>
      <% end %>

      <!-- Before moving Devise Folder to Users -->
      <%#= link_to 'log in', new_user_session_path, class:'text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !current_user %>

      <%#= link_to 'sign up', new_user_registration_path, class:'text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !current_user %>

      <%#= link_to "Logout", destroy_user_session_path, method: :delete, class:"text-gray-300 hover:bg-gray-700 hover:text-white px-4 py-2 rounded-md text-sm font-medium" if user_signed_in? %>


      <!-- After moving Devise Folder to Users -->
      <%= link_to 'log in', sign_in_path, class:'text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !current_user %>

      <%= link_to 'sign up', sign_up_path, class:'text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium' if !current_user %>

      <%= link_to "Logout", signout_path, method: :delete, class:"text-gray-300 hover:bg-gray-700 hover:text-white px-4 py-2 rounded-md text-sm font-medium" if user_signed_in? %>

    </li>

  </div>
</nav>

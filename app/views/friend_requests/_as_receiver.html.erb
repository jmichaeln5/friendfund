<div class="text-2xl"> <%= "#{current_user.friend_requests_as_receiver.count} Requests Received" %>
  <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#friendRequestAsReceiverModal">
    Received
  </button>
</div>

<!-- As receiver Modal -->
<div class="modal fade" id="friendRequestAsReceiverModal" tabindex="-1" role="dialog" aria-labelledby="friendRequestAsReceiverModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="friendRequestAsReceiverModalLabel">Friend Requests Received</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
        <div class="modal-body">

          <% current_user.friend_requests_as_receiver.each do |friend_request| %>

          <div class="card p-4">

              <span class="text-xl">
                <%= link_to "#{friend_request.requestor.name}", user_path(friend_request.receiver) %> sent you a <%= link_to "friend request", friend_request_path(friend_request) %>
                <br>
                <%= "Request: #{friend_request.status.capitalize}" %> <%= get_friendship_status(friend_request) %>
                <br>
                <p class="text-sm text-gray-800"> <%=  "#{time_ago_in_words(friend_request.created_at)}" %> </p>
                <hr>
             </span>

              <div>
                <%= link_to 'Destroy', friend_request, method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
            </div>

          <% end %>

        </div>

        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>

    </div>
  </div>
</div>

<%= render "notifications/unread_notifications" %>
<%= render "notifications/earlier_notifications" %>

<!-- No Notifications -->
<% if (Notification.all.where(recipient: current_user.id).any? == false) %>

<p class=" text-2xl text-center">
  no notifications available
</p>

<% end %>
